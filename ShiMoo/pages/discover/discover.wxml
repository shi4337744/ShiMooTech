<view class="discover-container">
  <view class="sticky-header">
    <scroll-view scroll-x class="tab-scroll">
      <view class="tab-item {{filterType === 'all' ? 'active' : ''}}" bindtap="changeFilter" data-type="all">全部动态</view>
      <view class="tab-item {{filterType === 'wall' ? 'active' : ''}}" bindtap="changeFilter" data-type="wall">校园墙</view>
      <view class="tab-item {{filterType === 'errand' ? 'active' : ''}}" bindtap="changeFilter" data-type="errand">跑腿</view>
      <view class="tab-item {{filterType === 'market' ? 'active' : ''}}" bindtap="changeFilter" data-type="market">闲置</view>
      <view class="tab-item {{filterType === 'food' ? 'active' : ''}}" bindtap="changeFilter" data-type="food">点评</view>
    </scroll-view>
  </view>

  <view class="feed-flow">
    <view class="empty-hint" wx:if="{{discoveryList.length === 0}}">
      <view class="empty-icon">🍃</view>
      <text>空空如也，去发布第一条动态吧</text>
    </view>
    
    <block wx:for="{{discoveryList}}" wx:key="id">
      <view class="feed-card fade-in" wx:if="{{filterType === 'all' || filterType === item.type}}">
        <view class="card-header">
          <view class="u-box">
            <view class="u-avatar">{{item.type === 'food' ? '🍱' : (item.type === 'market' ? '🎁' : '👤')}}</view>
            <view class="u-meta">
              <text class="u-name">{{item.userName}}</text>
              <text class="u-time">{{item.time}} · {{item.location}}</text>
            </view>
          </view>
          <view class="badge tag-{{item.type}}">
            {{item.type === 'wall' ? '校园墙' : (item.type === 'errand' ? '跑腿' : (item.type === 'market' ? '闲置' : '点评'))}}
          </view>
        </view>

        <view class="card-content">
          <text class="text">{{item.content}}</text>
          <image wx:if="{{item.imageUrl}}" class="img" src="{{item.imageUrl}}" mode="widthFix" lazy-load></image>
          
          <view class="price-bar" wx:if="{{item.type === 'errand' || item.type === 'market'}}">
            <text class="price-val">￥{{item.price}}</text>
            <text class="type-hint">{{item.type === 'errand' ? '任务悬赏' : '转让价'}}</text>
          </view>
        </view>

        <view class="card-footer">
          <view class="btn">❤️ 赞</view>
          <view class="btn">💬 评论</view>
          <view class="btn">🚀 分享</view>
        </view>
      </view>
    </block>
  </view>
</view>